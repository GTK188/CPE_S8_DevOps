all:
 vars:
   ansible_user: centos
   ansible_ssh_private_key_file: ansible-vault decrypt ~/.ssh/id_rsa --vault-password-file ~/.ssh/vault_password

   POSTGRES_DB: "db"
   POSTGRES_USER: "usr"
   POSTGRES_PASSWORD: "pwd"
   POSTGRES_HOST: "database:5432"
 children:
   prod:
     hosts: loan.aubry.takima.cloud
